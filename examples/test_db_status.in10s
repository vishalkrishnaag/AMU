# DB status smoke test.
#
# This does not require a running PostgreSQL server. It only verifies that the
# VM recognizes the DBSTATUS opcode and reports no active connection.
#
# Run:
#   ./intense.out examples/test_db_status.in10s main 4

main:
    DBSTATUS
    PRINTJ
    RET
